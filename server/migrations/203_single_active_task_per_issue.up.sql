CREATE UNIQUE INDEX CONCURRENTLY idx_one_active_task_per_issue ON agent_task_queue(issue_id) WHERE issue_id IS NOT NULL AND trigger_comment_id IS NULL AND is_leader_task = FALSE AND trigger_evidence_kind = 'issue_assignment' AND status IN ('queued', 'dispatched', 'running', 'waiting_local_directory', 'deferred');
