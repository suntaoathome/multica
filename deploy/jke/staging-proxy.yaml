apiVersion: v1
kind: ConfigMap
metadata:
  name: handoff-staging-nginx
  namespace: handoff-staging
  labels:
    app.kubernetes.io/name: multica
    app.kubernetes.io/instance: handoff-staging
    app.kubernetes.io/component: public-proxy
data:
  nginx.conf: |
    events {}

    http {
      map $http_upgrade $connection_upgrade {
        default upgrade;
        '' close;
      }

      server {
        listen 8080;
        server_name handoff-staging.oxygent.org.cn;
        return 301 https://$host$request_uri;
      }

      server {
        listen 8081;
        server_name handoff-staging.oxygent.org.cn;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;

        location = /healthz {
          proxy_pass http://handoff-staging-backend:8080;
        }

        location = /api/daemon/ws {
          proxy_http_version 1.1;
          proxy_set_header Upgrade $http_upgrade;
          proxy_set_header Connection $connection_upgrade;
          proxy_pass http://handoff-staging-backend:8080;
        }

        location /api/ {
          proxy_pass http://handoff-staging-backend:8080;
        }

        location /auth/ {
          proxy_pass http://handoff-staging-backend:8080;
        }

        location /uploads/ {
          proxy_pass http://handoff-staging-backend:8080;
        }

        location /ws {
          proxy_http_version 1.1;
          proxy_set_header Upgrade $http_upgrade;
          proxy_set_header Connection $connection_upgrade;
          proxy_pass http://handoff-staging-backend:8080;
        }

        location / {
          proxy_pass http://handoff-staging-frontend:3000;
        }
      }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: handoff-staging-proxy
  namespace: handoff-staging
  labels:
    app.kubernetes.io/name: multica
    app.kubernetes.io/instance: handoff-staging
    app.kubernetes.io/component: public-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: multica
      app.kubernetes.io/instance: handoff-staging
      app.kubernetes.io/component: public-proxy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: multica
        app.kubernetes.io/instance: handoff-staging
        app.kubernetes.io/component: public-proxy
    spec:
      imagePullSecrets:
        - name: jcr-pull-secret
      containers:
        - name: nginx
          image: oxygent-cn-north-1.jcr.service.jdcloud.com/oxygent/nginx:1.27-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
            - name: app-http
              containerPort: 8081
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
              readOnly: true
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
      volumes:
        - name: nginx-config
          configMap:
            name: handoff-staging-nginx
---
apiVersion: v1
kind: Service
metadata:
  name: handoff-staging-public
  namespace: handoff-staging
  labels:
    app.kubernetes.io/name: multica
    app.kubernetes.io/instance: handoff-staging
    app.kubernetes.io/component: public-proxy
  annotations:
    service.beta.kubernetes.io/jdcloud-load-balancer-spec: |
      version: "v1"
      loadBalancerType: alb
      listeners:
        - protocol: "HTTP"
        - protocol: "HTTPS"
          certificateId: "cert26071552d09f2722cd328f"
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: multica
    app.kubernetes.io/instance: handoff-staging
    app.kubernetes.io/component: public-proxy
  ports:
    - name: http
      port: 80
      targetPort: 8080
    - name: https
      port: 443
      targetPort: 8081
