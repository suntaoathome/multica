# Mobile production env. These are public endpoints, never credentials.
# Loaded by the `*:prod` scripts via dotenv-cli (see package.json).
EXPO_PUBLIC_API_URL=https://handoff.oxygent.org.cn
EXPO_PUBLIC_WEB_URL=https://handoff.oxygent.org.cn
